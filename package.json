{
  "name": "attendances-project",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "rebuild": "tsc -b --clean && tsc -b && vite build",
    "lint": "eslint .",
    "preview": "tsc -b && vite build && vite preview",
    "deploy": "gh-pages -d dist",
    "br-deploy": "tsc -b --clean && tsc -b && vite build && gh-pages -d dist"
  },
  "dependencies": {
    "@emotion/react": "^11.14.0",
    "@emotion/styled": "^11.14.1",
    "@mui/icons-material": "^7.3.2",
    "@mui/material": "^7.3.2",
    "@mui/x-charts": "^8.12.0",
    "@tailwindcss/vite": "^4.1.13",
    "@vitejs/plugin-react": "^5.0.4",
    "date-fns": "^4.1.0",
    "react-error-boundary": "^6.0.0",
    "react-router": "^7.9.3",
    "rsuite": "^5.83.3",
    "tailwindcss": "^4.1.13",
    "zustand": "^5.0.8"
  }
}
